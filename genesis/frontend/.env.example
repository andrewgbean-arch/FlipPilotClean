# Backend API base. Use "/api" to go through the Vite dev proxy (or nginx in Docker).
VITE_GENESIS_API=http://127.0.0.1:8000/api
# Optional: sent as X-Genesis-Token when the backend has GENESIS_API_TOKEN set.
# VITE_GENESIS_TOKEN=
